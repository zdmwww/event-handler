package site.autzone.event.handler.task;
import groovy.lang.Script;
public class BaseScript extends Script {
  
  @Override
  public Object run() {
    def taskEvent =  this.getBinding().getVariable("taskEvent");
    %s
  }
}